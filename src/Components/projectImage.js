import React from "react";
import "bootstrap";
import { Image } from "react-bootstrap";
import { Tournament } from "../assets/images/tournament.PNG";

const ProjectImage = (props) => {
  return (
    // <Image src={Tournament} className="mt-5" alt="tournament" fluid={true} />
    <h5>Hello</h5>
  );
};

export { ProjectImage };
